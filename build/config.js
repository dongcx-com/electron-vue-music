const publicPath = 'electron-vue-music'
const outputDir = 'electron-vue-music'

// build config set
module.exports = {
    // dev server
    host: '127.0.0.1',
    port: '8002',
    publicPath: publicPath,
    outputDir: outputDir,
}